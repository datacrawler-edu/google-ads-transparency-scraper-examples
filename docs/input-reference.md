# Input reference

The input is documented by the Actor's live [Input schema](https://apify.com/datascraperes/google-ads-transparency-scraper?fpr=edudata). The main fields are `searchQuery`, `maxResults`, `platform`, `adFormat`, `region`, `dateFrom`, `dateTo`, `maxPages`, `maxAdvertisers`, `includeDetails`, and `maxDetails`.
